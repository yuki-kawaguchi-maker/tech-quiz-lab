# CLAUDE.md

## このプロジェクトについて
- ユーザー(yuki)専用の4択クイズアプリ。顧客データ・Salesforce組織を持たない個人アプリ。
- 詳細な決定事項は `STATE.md`「ユーザーによる業務判断の記録」を参照。

## 制約(必ず守る)
- 外部ライブラリ・APIキーを使わない。1枚HTML(`index.html`)+ 問題データJSON
  (`data/questions/*.json`)の静的構成のみ。GitHub Pagesで配信する。
- 問題データは `docs/question-schema.md` のスキーマに従う。新しいカテゴリ・問題を
  追加する際は、このスキーマを変更せず追記する(スキーマ変更が必要な場合は
  `docs/question-schema.md` を先に改訂してからデータを作成する)。
- UIはスマホのブラウザ幅380pxで破綻しないこと。
- 図解SVGは「箱2〜3個+矢印+ラベル+ひと言キャプション」の型に限定し、要素を増やさない。
- 誤答の選択肢は「明らかに違う」ものではなく「惜しい・混同しやすい概念」で構成する。
- localStorageへの読み書きは必ずtry/catchで保護する(プライベートブラウジング等で
  アクセスできない場合にアプリが落ちないようにする)。

## 出力・セッション運用ルール
- 報告は結果サマリー+結論のみ。diff・ログ全文・SVG本文・JSON全文をチャットに貼らない。
- 業務判断(カテゴリ構成・出題基準・配信方式など)を新たに決めた場合は、着手前に
  `STATE.md`「ユーザーによる業務判断の記録」に判断日・判断者・論点・決めた内容・理由を
  記録してから実装する。
